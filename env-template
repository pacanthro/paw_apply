export DJANGO_SECRET_KEY="generate a very lonnnnnnnng secrete key to use fod salting."
export DEBUG="True"
export DB_ENGINE="django.db.backends.postgresql" # Or DB_ENGINE="django.db.backends.mysql"
export DB_USER="username"
export DB_PASSWORD="password"
export EMAIL_BACKEND="django.core.mail.backends.filebased.EmailBackend"
